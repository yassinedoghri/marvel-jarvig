import styled from "styled-components";

const Title = styled.h2`
  font-family: Bangers, sans-serif;
  padding-left: ${props => props.theme.spaces.md}em;
  color: white;
  letter-spacing: 0.1em;
`;

export default Title;
